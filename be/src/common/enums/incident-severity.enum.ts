// Mức nghiêm trọng — server TỰ ĐỘNG suy từ category + Σclaimed (INCIDENT_SEVERE_CRITERIA),
// không để khách tự khai.
export enum IncidentSeverity {
  CRITICAL = 'CRITICAL',
  MAJOR = 'MAJOR',
  MINOR = 'MINOR',
}
